// neo-map/badges.js
// Compatibility stub so app.js can import buildAreaBadges without rendering anything.
// Returns an empty list; no markers or layers are added.

export function buildAreaBadges(map, poles, byKey) {
  return [];
}
